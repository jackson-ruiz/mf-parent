import { render, screen, waitFor } from "@testing-library/react";
import React from "react";
import Button from "./Button";

describe("Group of test for Button ", () => {
  test("should render correctly the component", async () => {
    // arrange
    // act
    // assert
    render(<Button />);
  });
});
